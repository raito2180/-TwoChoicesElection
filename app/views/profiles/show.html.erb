<div class="bg-blue-900 w-full h-screen text-white overflow-hidden flex items-center justify-center">
  <div class='w-3/4 md:w-1/2 border-dotted border-t-2 border-fitting-whitec pt-2 flex flex-col items-center justify-center'>
    <div class="avatar">
      <div class="w-40 h-40 md:w-80 md:h-80 rounded-full flex items-center justify-center">
        <% if @profile.avatar.present? %>
        <%= image_tag @profile.avatar, size: '350x270' %>
        <% else %>
          <%= image_tag "default.jpg" %>
        <% end %>
      </div>
    </div><br>
    <div class="badge badge-lg badge-primary">
      <%= link_to 'プロフィール編集', edit_profile_path %>
    </div>
    <h1 class='font-bold text-2xl pt-2'>
      ユーザー名：<%= @profile.name %>
    </h1>
    <h1 class='font-bold text-2xl pt-2'>
      性別：<%= @profile.gender %>
    </h1>
    <h1 class='font-bold text-2xl pt-2'>
      ひとこと<br>
    </h1>
    <h1 class='md:w-1/2 font-bold text-2xl pt-2 pb-2 flex items-center justify-center'>
      <%= @profile.body %>
    </h1>
    <h1 class='w-full font-bold text-2xl border-dotted border-b-2 border-fitting-white pt-2 pb-2'>
    </h1>
  </div>
</div>

